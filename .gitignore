# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules
node_modules

# testing
/coverage

# production
/build
# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local Netlify folder
.netlify

# puppeteer's downloaded Chromium (see netlify.toml)
.cache

# public/Darshan_Patel_Resume.pdf is NOT ignored: it is rebuilt from
# public/resumeData.json by scripts/build-resume.js, but stays committed so a
# CI box that cannot launch Chromium still has a PDF to publish.
